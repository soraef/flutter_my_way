name: fire_repo
description: This package provides an implementation of the Repository pattern with Firestore as the backend.
version: 0.0.3
repository: https://github.com/soraef/flutter_my_way/tree/master/packages/fire_repo

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^1.13.1
  cloud_firestore: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter: